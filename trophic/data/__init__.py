"""Static data tables used across the trophic stack.

Each module exposes immutable Python constants (tuples / frozensets) so
they're safe to import at module load time and cheap to share across
processes.
"""
